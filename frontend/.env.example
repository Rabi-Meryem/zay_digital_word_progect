# Copier ce fichier en ".env" (non versionné) et ajuster si besoin.
# Base URL de l'API Django. Vérifier le préfixe réel dans backend/config/urls.py
# (ex: si les routes sont incluses via path('api/', include('users.urls')), garder "/api" ;
# sinon l'enlever).
VITE_API_URL=http://localhost:8000/api
